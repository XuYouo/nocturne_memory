main:
  push:
    - services:
        - docker
      stages:
        # 该仓库是多容器项目：backend 与 frontend 分别在独立目录下构建。
        - name: docker build
          script: |
            docker build -t ${CNB_DOCKER_REGISTRY}/${CNB_REPO_SLUG_LOWERCASE}-backend:latest ./backend
            docker tag ${CNB_DOCKER_REGISTRY}/${CNB_REPO_SLUG_LOWERCASE}-backend:latest ${CNB_DOCKER_REGISTRY}/${CNB_REPO_SLUG_LOWERCASE}-backend-sse:latest
            docker build -t ${CNB_DOCKER_REGISTRY}/${CNB_REPO_SLUG_LOWERCASE}-frontend:latest ./frontend
        - name: docker push
          script: |
            docker push ${CNB_DOCKER_REGISTRY}/${CNB_REPO_SLUG_LOWERCASE}-backend:latest
            docker push ${CNB_DOCKER_REGISTRY}/${CNB_REPO_SLUG_LOWERCASE}-backend-sse:latest
            docker push ${CNB_DOCKER_REGISTRY}/${CNB_REPO_SLUG_LOWERCASE}-frontend:latest

$:
  vscode:
    - docker:
        image: docker.cnb.cool/o.k/dev-env-dev:latest
      services:
        - vscode
        - docker
      # 开发环境启动后会执行的任务
      stages:
        - name: setup anthropic env
          imports: 
            - https://cnb.cool/o.k/secrets/-/blob/main/claude.yml
            - https://cnb.cool/o.k/secrets/-/blob/main/codex.yml
          script: |
            # 创建 .claude 目录
            mkdir -p .claude
            mkdir -p ~/.codex
            cat > ~/.claude.json << EOF
            {
              "hasCompletedOnboarding": true
            }
            EOF
            # 创建 settings.json 文件
            cat > .claude/settings.json << EOF
            {
              "env": {
                "ANTHROPIC_AUTH_TOKEN": "${ANTHROPIC_AUTH_TOKEN}",
                "ANTHROPIC_BASE_URL": "${ANTHROPIC_BASE_URL}",
                "API_TIMEOUT_MS": "3000000",
                "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": 1
              },
              "alwaysThinkingEnabled": true
            }
            EOF
            echo ".claude/settings.json"
            cat > ~/.codex/config.toml << EOF
            model_provider = "octopus"
            model = "gpt-5.3-codex"
            model_reasoning_effort = "high"
            disable_response_storage = true
            [model_providers.octopus]
            name = "octopus"
            base_url = "${OPENAI_BASE_URL}"
            wire_api = "responses"
            requires_openai_auth = true
            EOF
            cat > ~/.codex/auth.json << EOF
            {
              "OPENAI_API_KEY": "${OPENAI_API_KEY}"
            }
            EOF
        - name: ls
          script: ls -al